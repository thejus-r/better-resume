const EmptyContent = () => {
  return <div className="w-full text-xs text-gray-400 font-mono h-20 flex items-center justify-center bg-gray-50 border border-dashed border-gray-300 rounded-xl">
    It looks pretty empty!
  </div>
}

export { EmptyContent }
